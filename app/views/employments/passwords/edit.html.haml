%h2 Cambiar contraseña
%br
.login-container
  .row
    .col-sm-8
      = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f|
        = devise_error_messages!
        = f.hidden_field :reset_password_token

        .row#pwd-container
          .col-xs-6
            .form-group
              = label_tag(:password, {},{class: "sr-only"})
              = f.password_field :password, class: 'form-control', autocomplete: 'off', placeholder: "Contraseña"
            .form-group
              = label_tag(:password, {},{class: "sr-only"})
              = f.password_field :password_confirmation, class: 'form-control', autocomplete: 'off', placeholder: "Confirmar contraseña"
          .col-xs-6
            .pwstrength_viewport_progress
            .pwstrength_viewport_errors

        = f.submit "Cambiar contraseña", class: 'center-block btn btn-blue btn-lg'