= render partial: 'layouts/employments/breadcrumb'
.container
  .job-detail
    .job-detail-header
      .row
        .col-xs-5
          .clip
            = image_tag("employments/icon-clip.png")
          .yellow
            = @job.institution_code
        .col-xs-7
          %div{id: "shareButtonLabelCount#{@job.id}"}
          :javascript
            $("#shareButtonLabelCount#{@job.id}").jsSocials({
              url: "#{employments_job_url(@job)}",
              text: "#{@job.post_name}",
              showCount: true,
              showLabel: true,
              shares: ["twitter", "facebook", "googleplus", "linkedin"]
            })
          %h2
            = @job.post_name

    .row
      .col-xs-12
        %p
          Para aplicar o consultar información sobre esta plaza, puede comunicarse haciendo uso de la siguiente información de contacto:
        %h4
          Datos de contacto
        %p
          = "Institución: "
          = @job.institution_name
        - if @job.email.present?
          %p
            = "Correo: "
            = mail_to @job.email
        %p
          = "Direccion: "
          = @job.address
        %hr
