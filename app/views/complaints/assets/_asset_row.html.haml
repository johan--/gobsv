%tr
  %td
    = check_box_tag 'asset_ids[]', asset_row.id, action_name.to_s == 'create'
  %td
    = asset_row.asset_file_name
  %td
    = number_to_human_size(asset_row.asset_file_size)
  %td
    = asset_row.created_at.try(:strftime, '%d/%m/%Y')
